from flatbread.config import config


TOTALS_NAME    = config['aggregation']['totals_name']
SUBTOTALS_NAME = config['aggregation']['subtotals_name']
NDIGITS        = config['aggregation']['ndigits']
LABELS_ABS     = config['aggregation']['label_abs']
LABELS_REL     = config['aggregation']['label_rel']
