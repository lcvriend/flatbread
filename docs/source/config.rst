Configuration
=============

Flatbread's default behavior can be modified with :class:`flatbread.config.Config`. It is possible to save your settings (and also to reset to the default). This is flatbread's default configuration:

.. literalinclude:: ../../flatbread/config.defaults.json
